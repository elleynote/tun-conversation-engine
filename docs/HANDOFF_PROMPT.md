# Handoff prompt for another coding session

Use this prompt together with this repository:

> Continue development of the Tun Conversation Engine. Read README.md, docs/ARCHITECTURE.md, docs/SETUP_CHECKLIST.md and the Supabase migrations before changing architecture. Preserve the existing state machine and source-adapter model. Do not add Zapier as a core dependency. Do not implement autonomous public posting without an explicit approval step unless the project owner later changes that decision. Work from the existing code, make minimal targeted changes, run `npm run typecheck` and `npm run build`, and report exactly what changed and what credentials are still missing.
